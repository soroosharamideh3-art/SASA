جواب سوال 1.

آپشن -c به کامپایلر اعلام می‌کند که فقط مرحله کامپایل و اسمبل را انجام دهد و وارد مرحله لینک کردن (Linking) نشود.

کاربرد1: برای زمانی که می‌خواهید فایل‌های منبع (Source files) را جداگانه کامپایل کنید و بعداً آن‌ها را به هم متصل کنید.

کاربرد2:زمانی که فایلی قبلا کامپایل شده و تغییری نکرده، و ما بخواهیم دوباره آنرا کامپایل کنیم، زمان برای کامپایل دوباره هدر نخواهد رفت

خروجی: یک فایل با پسوند o. filename ایجاد می‌کند که شامل کد ماشین است اما هنوز به توابع کتابخانه‌ای متصل نشده و قابل اجرا نیست.

جواب سوال 2.

هر دو برای دیباگ کردن کد هستند

Step : خط به خط کد را در زمان اجرای آن  پیمایش می‌کند، در هنگام برخورد با فراخوانی تابع، وارد تابع شده و خط به خط آن را طی میکند

` `Next:خط به خط کد را در زمان اجرای آن پیمایش می‌کند، در هنگام برخورد با فراخوانی تابع، وارد تابع نشده و نتیجه را فقط اعمال میکند

از  step زمانی استفاده می‌کنیم که از درستی تابع خود مطمئن نیستیم و از  next زمانی استفاده می‌کنیم که از درستی تابع خود مطمئن هستیم

جواب سوال 3.

func.c:

#include <stdio.h>

void say\_hello() {

`    `printf("Hello from helper.c!\n");

}

main.c:

void display\_message();

int main() {

`    `display\_message();

`    `return 0;

}

Makefile:

CC=gcc

CFLAGS=-Wall -g

all: my\_app

my\_app: main.o func.o

`	`$(CC) main.o func.o -o my\_app

main.o: main.c

`	`$(CC) $(CFLAGS) -c main.c

func.o: func.c

`	`$(CC) $(CFLAGS) -c func.c

clean:

`	`rm -f my\_app

کد ما باید فایل‌های .o را با هم ترکیب کرده و فایل نهایی 'my\_program' را بسازد

اما هنگام ادغام و کامپایل این دو، فایل های .oموجود نیستند، پس کد مولد آنها را قرارمیدهیم که فایل های مورد نیاز ما را به صورت حاصلی از کامپایل بدون لینکینگ بدهد

در نهایت کد میک فایل ما بصورت بازگشتی ادغام دو فایل را با لینکینگ میسازد
